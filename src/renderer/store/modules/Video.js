const state = {
  videoInfo: {},
}
const getters = {};
const mutations = {
  SET_VIDEO_INFO (state, payload) {
    state.videoInfo = payload;
  },
};
const actions = {};
export default {
  state,
  getters,
  mutations,
  actions
};
